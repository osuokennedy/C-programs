#include <stdio.h>
#include <stdlib.h>

int main()
{
	char *command = NULL;
	size_t len = 0;
	ssize_t read;
	
while (read)
      {
	write(1, "$ ", 2);
	read = getline(*command, &len, stdin);
	printf("%s", command);

	}
	free(command);
	return (0);
}
